public class MagicSquare {

    private int n;
    private int[][] magicGrid;
    private int magicNumber;

    public MagicSquare(int n) {
        magicNumber = n * (n*n + 1) / 2;
        magicGrid = new int[n][n];
        for (int r = 0;r < n;r++) {
            for (int c=0;c<n; c++) {
                while (magicGrid==null) {
                    
                }
            }
        }
    }
}